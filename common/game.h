#ifndef GAME_H
#define GAME_H

/* Runs the game forever: title screen, then bomb after bomb until an
   explosion sends the player back to the title screen. Never returns. */
extern void game_run(void);

#endif /* GAME_H */
