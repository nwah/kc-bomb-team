#ifndef VERSION_H
#define VERSION_H

/* The release number, shown in the top-right corner of the title screen on
   both ports. Bump it here and nowhere else; the Makefile rebuilds both
   targets when this file changes. */
#define VERSION "v0.2"

#endif /* VERSION_H */
